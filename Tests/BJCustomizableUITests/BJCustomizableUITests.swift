import XCTest
@testable import BJCustomizableUI

final class BJCustomizableUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BJCustomizableUI().text, "Hello, World!")
    }
}
